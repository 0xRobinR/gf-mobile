import 'package:flutter/material.dart';

class StorageProviderItem extends StatelessWidget {
  const StorageProviderItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const SizedBox(height: 10),
        const Placeholder(),
        const SizedBox(height: 10),
      ],
    );
  }
}
